<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>All Recipes</title>
  </head>
  <body>
    <% @recipes.each do |recipe| %>
      <p><a href=<%="/recipes/#{recipe.id}"%>>Name: <%= recipe.name %></a></p>
    <% end %>
  </body>
</html>
